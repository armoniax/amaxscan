import { FC, memo, ReactElement } from "react";
import Baseweb from "@/components/baseContainer/webwrap";
import "./index.scss";


const BlockDetail: FC = (): ReactElement => {
  return (
    <div className="block-detail">

    </div>
  );
};

export default Baseweb(memo(BlockDetail));
